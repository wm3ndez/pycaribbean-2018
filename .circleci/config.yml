version: 2
jobs:
   build:
      docker: circleci/python:3.6.4
      steps:
         - checkout
         - run:
            command: |
               pip install -r requirements.txt
         - run:
            command: |
               python manage.py test blog
